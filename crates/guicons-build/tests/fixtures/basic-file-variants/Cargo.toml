[package]
name = "guicons-fixture-basic-file-variants"
version = "0.0.0"
edition = "2024"
publish = false

[workspace]

[dependencies]
guicons = { path = "../../../../guicons" }

[build-dependencies]
guicons-build = { path = "../../../../guicons-build" }
