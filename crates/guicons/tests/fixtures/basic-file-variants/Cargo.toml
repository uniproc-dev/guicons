[package]
name = "guicons-fixture-basic-file-variants"
version = "0.0.0"
edition = "2024"
publish = false

[workspace]

[dependencies]
guicons = { path = "../../.." }

[build-dependencies]
guicons = { path = "../../..", features = ["build"] }
